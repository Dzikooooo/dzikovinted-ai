# Memes valeurs que le .env du projet principal (racine du repo), Settings > API
# dans le dashboard Supabase. Duplique ici car extension/ est un paquet Vite
# independant (pas de workspace partage, voir EXTENSION.md §9).
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
