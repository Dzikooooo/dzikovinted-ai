# Base de données

Postgres via Supabase. Schéma versionné dans `supabase/migrations/`, appliqué via `npx supabase db push` (voir [README.md](README.md)).

## ⚠️ Piège connu : dérive schéma réel ↔ migrations versionnées

Plusieurs changements de schéma en prod (des policies RLS notamment) ont été appliqués hors du flux `supabase db push` — probablement collés directement dans le SQL Editor du dashboard — et n'apparaissaient donc dans aucune migration du repo. Deux conséquences rencontrées en juillet 2026 :

1. **L'historique de migration de la CLI était désynchronisé.** `supabase db push` essayait de rejouer une migration déjà appliquée en prod (colonnes/tables `if not exists`, donc inoffensif) jusqu'à tomber sur un `create policy` sans garde, qui plantait tout le push. Fix : `supabase migration repair --status applied <version>` pour resynchroniser l'historique avant de repush.
2. **Des policies RLS non versionnées ont été trouvées en auditant la prod** (`supabase db query --linked "SELECT * FROM pg_policies WHERE ..."`) qui n'existaient dans aucun fichier de migration — dont une policy `ALL` pour le rôle `anon` sur `market_opportunities` (n'importe qui pouvait écrire/supprimer sans être connecté). Voir plus bas.

**Leçon : ne jamais modifier le schéma de prod directement dans le dashboard.** Toujours passer par une migration versionnée + `db push`, même pour un changement d'une ligne. Si un doute existe sur l'état réel de la prod, vérifier avec `npx supabase db query --linked "..."` (lecture seule, sûr) plutôt que de faire confiance aux fichiers de migration seuls.

**Même piège pour les Edge Functions** : une fonction `scan-market` est restée déployée sur Supabase jusqu'au 2026-07-11 sans jamais avoir été trackée dans ce dépôt Git. C'était le déploiement Supabase de l'ancien `api/scan-market.ts`, un « faux scanner » à données codées en dur, explicitement remplacé par le vrai scraper Playwright (`scripts/vinted-scan.ts`) dans le commit `c8b7905` (2026-07-07) — mais jamais supprimé côté Supabase quand son code source local a disparu. Aucune fausse donnée n'a jamais atteint la table `market_opportunities` (vérifié : les 309 lignes en prod correspondent toutes à de vraies URLs Vinted à identifiant numérique), mais la fonction restait appelable et aurait pu en insérer. Supprimée via `npx supabase functions delete scan-market`. **Leçon : `npx supabase functions deploy` déploie, mais rien ne supprime automatiquement une fonction quand son code source est retiré du dépôt — un `supabase functions list` périodique, recoupé avec les dossiers réellement trackés dans `supabase/functions/`, est le seul moyen fiable de détecter ce genre de dérive.**

## ⚠️ Piège connu : un trigger BEFORE INSERT ne peut pas faire référence à sa propre ligne via une table enfant (FK)

Bug réel confirmé le 2026-07-27 (remonté par l'utilisateur : une annonce Vinted réelle disparaissait de ResellOS après synchro, aucune erreur visible nulle part). `trg_assign_sku_before_insert` (`BEFORE INSERT` sur `listings`, créé le 2026-07-13) appelait à l'origine `allocate_next_sku()` — un simple calcul en lecture sur `listings`, inoffensif en `BEFORE INSERT`. Le 2026-07-26, sa fonction sous-jacente a été remplacée (`CREATE OR REPLACE FUNCTION`, sans jamais recréer le trigger lui-même) pour appeler `allocate_sku()` à la place — qui, lui, **insère dans `listing_skus` en référençant `listing_id = NEW.id`**. En contexte `BEFORE INSERT`, la ligne `listings` elle-même n'existe pas encore dans la table (elle n'est écrite qu'après le retour du trigger) — la contrainte de clé étrangère `listing_skus_listing_id_fkey` échouait donc systématiquement (`23503`) dès qu'un nouvel enregistrement était inséré sans `sku` déjà fourni (synchro passive en masse, import, Générateur). Reproduit à l'identique dans une transaction annulée avant correctif (`SET LOCAL request.jwt.claims` + `ROLLBACK`, voir plus bas) pour confirmer la cause exacte avant de toucher au schéma.

**Leçon générale : un trigger `BEFORE INSERT`/`BEFORE UPDATE` ne peut jamais déclencher, directement ou via une fonction imbriquée, un `INSERT` dans une autre table qui référence par clé étrangère la ligne en cours d'écriture — cette ligne n'existe pas encore pour d'autres requêtes tant que le trigger `BEFORE` n'a pas rendu la main.** Si un trigger a besoin d'écrire dans une table enfant liée par FK à la ligne qu'il traite, il doit être un trigger `AFTER` (la ligne est alors déjà visible dans la même transaction) — un `AFTER INSERT` ne peut pas modifier `NEW` via `RETURN`, donc toute correction de la ligne elle-même doit passer par un `UPDATE` explicite dans le corps du trigger. Fix appliqué dans `20260727160000_fix_sku_trigger_timing.sql` : `assign_sku_before_insert()` remplacé par `assign_sku_after_insert()` (`AFTER INSERT`), qui appelle `allocate_sku()` puis fait `UPDATE listings SET sku = ... WHERE id = NEW.id`.

## Tables

| Table | Rôle | RLS |
|---|---|---|
| `profiles` | Profil utilisateur (plan, crédits) | `auth.uid() = id`. **Depuis le 2026-07-11 (audit pré-lancement, P0.1) : le client ne peut plus écrire `plan`/`credits`/`email`/`avatar_url` — seul `full_name` reste modifiable par `authenticated` (`GRANT UPDATE (full_name)`, tout le reste a été révoqué au niveau colonne après un `REVOKE UPDATE` complet).** `plan`/`credits` ne sont modifiables que par les RPC `SECURITY DEFINER` `reserve_credit`/`refund_credit_reservation` (voir plus bas), qui tournent avec les privilèges du propriétaire de la fonction et contournent donc ce `REVOKE`. `CHECK (plan IN ('free','pro','team'))` et `CHECK (credits >= 0)` restaurés/ajoutés en garde-fou (voir piège de dérive de schéma ci-dessus — le premier avait disparu de la prod sans qu'aucune migration ne le supprime). **Colonne `role` ajoutée le 2026-07-13** (`'user'`/`'admin'`, défaut `'user'`) : hérite automatiquement du même verrouillage (`authenticated` n'a déjà aucun droit `UPDATE` hors `full_name`) — un compte `role='admin'` a des crédits illimités, vérifié côté serveur dans `analyze-clothing` (edge function), pas seulement côté frontend |
| `credit_reservations` | **Réservation transactionnelle de crédits IA** (P-02, 2026-08-04, remplace le couple `decrement_credit`/`refund_credit`) : une ligne par tentative de génération sur le plan `free`, `status` `pending`→`consumed` (génération réussie) ou `pending`→`refunded` (échec). Créée par `reserve_credit` (qui décrémente `profiles.credits` dans la même transaction), close par `consume_credit_reservation` (succès, ne touche jamais les crédits) ou `refund_credit_reservation` (échec, recrédite seulement si la ligne était encore `pending`). Le `reservation_id` n'est **jamais renvoyé au client** par `analyze-clothing` — il ne vit que dans le scope d'exécution serveur de l'edge function. **Revue de sécurité du 2026-08-04** : une première version donnait `authenticated` accès en lecture à ses propres réservations (policy SELECT) et exécution directe aux 3 RPC — un utilisateur pouvait alors lire le `reservation_id` d'une génération en cours et appeler `refund_credit_reservation` en parallèle depuis les DevTools. Corrigé avant tout déploiement : RLS activée **sans aucune policy** (deny total pour `authenticated`/`anon`, y compris en lecture), et les 3 RPC réservées à `service_role` uniquement — voir ci-dessous | Aucune policy, pour aucun rôle PostgREST. RLS activée = deny par défaut pour `select`/`insert`/`update`/`delete` côté `authenticated`/`anon`. Seul `service_role` (bypass RLS) et les fonctions `SECURITY DEFINER` y accèdent |
| `listings` | Annonces générées/en stock/vendues. **Fusionnée avec l'ancienne `vinted_listings` (2026-07-09)** : source de vérité unique pour un article, qu'il vienne du Générateur ou d'une synchro Vinted. Colonnes Vinted ajoutées : `vinted_account_id` (nullable — `null` = brouillon jamais publié), `vinted_item_id`, `vinted_url`, `vinted_status` (`online`/`reserved`/`sold_pending`/`sold_completed`/`draft`/`hidden`/`deleted`/`unknown`, distinct de `status` qui reste le statut workflow ResellOS), `brand`, `size`, `favourites`, `views`, `synced_at`. Index partiel unique `(vinted_account_id, vinted_item_id) where vinted_item_id is not null`. Colonne texte morte `account` supprimée. `vinted_status = 'deleted'` est une suppression douce — toute lecture doit exclure ces lignes avec `.or('vinted_status.neq.deleted,vinted_status.is.null')` (pas un simple `.neq`, qui exclurait aussi les lignes jamais liées à Vinted). **Colonne `last_edited_at` ajoutée le 2026-07-13** (`timestamptz`, nullable) : horodatage de la dernière édition manuelle depuis `EditListingModal.tsx` (`StockPage.tsx`), `null` tant que l'annonce n'a jamais été éditée après sa création. **Colonne `sku` ajoutée le 2026-07-13** (`integer`, nullable) : numéro unique par utilisateur (#1, #2, #43...), attribué automatiquement par le trigger `trg_assign_sku_before_insert` (appelle `allocate_next_sku`, voir RPC ci-dessous) — jamais géré manuellement côté client. Index partiel unique `(user_id, sku) where sku is not null`. Le titre stocké reste toujours propre ; le SKU n'est concaténé qu'au moment de l'envoi vers Vinted (`src/lib/sku.ts::formatTitleWithSku`). Voir `EXTENSION.md` §5 et §6.2bis pour les règles de propriété des champs (quels champs la synchro peut réécrire, quels champs l'édition manuelle peut toucher) et l'auto-comptabilité sur vente détectée | `auth.uid() = user_id`, CRUD complet |
| `usage` | Compteur d'analyses IA par mois, alimenté par le RPC `increment_usage` | `auth.uid() = user_id`. **Depuis le 2026-07-11 (P0.1) : `UPDATE` entièrement révoqué pour `authenticated`** (le client ne modifie jamais ce compteur directement, seul `increment_usage` y écrit) |
| `subscriptions` | Scaffolding pour un futur abonnement Stripe — **pas encore utilisée**, `SubscriptionPage.tsx` n'a aucune intégration Stripe réelle | `auth.uid() = user_id`, **lecture seule pour le propriétaire depuis le 2026-07-11 (P0.1)** — `INSERT`/`UPDATE`/`DELETE` révoqués pour `authenticated` par anticipation, avant même l'intégration Stripe. Toute écriture future viendra d'un webhook Stripe en `service_role` (qui contourne RLS/grants, donc aucun accès `authenticated` n'est nécessaire) |
| `expenses` | Dépenses de l'activité (emballage, port...) | `auth.uid() = user_id`, CRUD complet |
| `vinted_accounts` | **Entité centrale** (2026-07-09) : les comptes Vinted réels de l'utilisateur, un par compte détecté par l'extension (principal, secondaire, boutique...). Remplace `accounts` (carnet d'étiquettes mort, supprimé) et `vinted_connection` (limité à un seul compte, supprimée). Une ligne ne peut être créée que par détection réelle de l'extension, jamais manuellement — voir `EXTENSION.md` §5. Index partiel `unique (user_id) where is_default` (Phase B) : un seul compte par défaut par utilisateur, changé via la RPC `set_default_vinted_account` | `auth.uid() = user_id`, CRUD complet |
| `vinted_listings_deprecated_20260709` | **Dépréciée** (2026-07-09) : ancienne table miroir des annonces Vinted, renommée (pas droppée) après la fusion de ses données dans `listings`. Filet de sécurité temporaire, à supprimer pour de bon une fois la fusion confirmée stable en conditions réelles | RLS d'origine conservée telle quelle |
| `listing_metric_snapshots` | **Historique pour le moteur d'intelligence métier** (2026-07-09) : journal append-only (`views`/`favourites`/`price`/`vinted_status` + `captured_at`), un instantané par annonce à chaque synchro. Permet de calculer des tendances réelles (perte de visibilité) sans jamais en fabriquer une faute d'historique suffisant — voir `EXTENSION.md` §5 et `src/lib/insights/trends.ts` | `auth.uid() = user_id` via `listing_id in (select id from listings where user_id = auth.uid())`, select+insert seulement (pas de update/delete, c'est un journal) |
| `action_log` | **Journal du Action Engine** (2026-07-10) : une ligne par action déclenchée via `src/lib/actions/` (`kind`, `vinted_account_id`, `listing_id`, `status` `pending_confirmation`→`success`/`error`/`cancelled`/`not_implemented`, `current_step`, `preview`, `result_payload`/`error_message`, `duration_ms`). Remplace la conception `sync_jobs` jamais implémentée. Écrite par l'app web (insert dans `prepare()`, update dans `confirm()`/`cancel()`) pour la plupart des actions — l'extension n'y écrit pas. **Exception depuis le 2026-07-11** : `scan_market` tourne hors session utilisateur (déclenché via GitHub Actions), donc `scripts/vinted-scan.ts` écrit lui-même le statut terminal en `service_role` quand `ACTION_ID` est fourni — voir `ARCHITECTURE.md` §4.3. Lue en temps réel par le Centre des Actions via Supabase Realtime (table ajoutée à la publication `supabase_realtime`). Voir `EXTENSION.md` §5 et `ARCHITECTURE.md` §4.6/§4.7 | `auth.uid() = user_id`, select/insert/update (pas de delete) ; l'insert valide en plus que `vinted_account_id`/`listing_id` appartiennent à l'utilisateur |
| `action_log_entries` | **Journal détaillé du Centre des Actions** (2026-07-10) : append-only, une ligne par étape/message journalisé pour une action (`action_id`, `step`, `message`, `at`) — permet de rejouer l'historique complet d'une action après coup, la progression du port `action-progress` (§EXTENSION.md §6.0) étant sinon purement éphémère. Même discipline que `listing_metric_snapshots`. Écrite par l'app web (`useActionEngine.ts`) pour la plupart des actions, et par `scripts/vinted-scan.ts` en `service_role` pour `scan_market` (même exception que ci-dessus). Lue en temps réel par le Centre des Actions et par `ScanProgressModal` | `action_id in (select id from action_log where user_id = auth.uid())`, select+insert seulement (pas de update/delete, c'est un journal) |
| `watchlist` | Recherches Vinted à scanner (brand/model/seuils profit-roi). `priority` (1-3) était lue uniquement pour l'ordre de scan jusqu'au 2026-07-12 ; consommée depuis comme un vrai signal de score par `scripts/opportunity-engine/scoring.ts` (voir `ARCHITECTURE.md` §4.8). **Personnalisée par utilisateur depuis le 2026-07-12** (`user_id` nullable ajouté) : `user_id = null` = recherche plateforme (les 7 recherches de départ, lecture seule pour tous, protégées de toute modification côté client), `user_id = <uuid>` = recherche personnelle CRUD complet pour son propriétaire via `src/pages/dashboard/WatchlistPage.tsx`. Le scanner dédoublonne les paires (brand, model) identiques entre utilisateurs avant de scanner — voir `scripts/watchlistDedup.ts` et `ARCHITECTURE.md` §4.9 | `auth.uid() = user_id or user_id is null` en lecture ; insert/update/delete réservés à `auth.uid() = user_id` (jamais vrai pour les recherches plateforme) |
| `market_opportunities` | Résultats du scanner, ré-écrite en entier à chaque `npm run scan` (`scripts/vinted-scan.ts`, Playwright, cron GitHub Actions `.github/workflows/scan-market.yml`) — **seule source légitime**. Colonnes ajoutées le 2026-07-12 pour le moteur d'opportunités (`risk_level`, `breakdown`, `resale_days_min/max`, `resale_confidence`, `first_observed_at`, `competing_listings_count`) — toutes nullables, s'activent progressivement à mesure que `market_price_observations` accumule de l'historique, voir `ARCHITECTURE.md` §4.8 | Lecture authenticated + anon, écriture `service_role` only |
| `market_price_observations` | **Historique pour le moteur d'opportunités** (2026-07-12) : journal append-only (`vinted_url`, `brand`, `category`, `price`, `favourites`, `scanned_at`), une ligne par item pertinent scrapé à chaque scan — que l'item devienne ou non une opportunité. Donne enfin une mémoire dans le temps à `market_opportunities`, qui était intégralement recréée à chaque scan sans laisser aucune trace. **Index `market_price_observations_scanned_at_idx (scanned_at desc)` ajouté le 2026-07-12** — le filtre réel de `scripts/vinted-scan.ts` (`.gte("scanned_at", ...)`, fenêtre 60 jours) ne correspondait à aucun des 3 index existants (tous avec `scanned_at` en second, jamais en tête), un balayage séquentiel complet dont le coût grandit avec le volume total de la table. Requête aussi plafonnée (`.limit(20000)`) en défense contre une croissance non purgée. Voir `ARCHITECTURE.md` §4.8.8 | Lecture authenticated uniquement (pas d'anon, donnée de marché globale mais écriture réservée à `service_role`), select+insert seulement (pas de update/delete, c'est un journal) |
| `opportunity_favourites` | **Favoris utilisateur sur les opportunités** (2026-07-12) : `market_opportunities` est intégralement supprimée/réécrite à chaque scan et n'a pas de colonne `user_id` — une colonne "favori" dessus serait donc effacée toutes les ~4h. Table dédiée, clé unique `(user_id, vinted_url)` — volontairement sur `vinted_url` et pas `market_opportunities.id` (qui change à chaque scan), pour que le favori survive à la disparition/réapparition de l'annonce entre deux scans | `auth.uid() = user_id`, select/insert/delete (pas d'update, un favori se retire et se recrée) |
| `scan_runs` | **Trace de chaque exécution de `scripts/vinted-scan.ts`** (2026-07-24, audit du parcours Scanner) : `status` (`running`/`success`/`error`), `triggered_by` (`cron`/`manual`), `opportunities_found`, `failed_searches`, `error_message`. Contrairement à `action_log` (per-user, réservé aux scans manuels via `ACTION_ID`), écrite pour **chaque** run — y compris le cron 4h, qui n'avait auparavant strictement aucune trace en cas d'échec après le `DELETE` de `market_opportunities` : un bêta-testeur voyait le même écran vide qu'un simple manque de données. Lue par `Opportunities.tsx` pour afficher "Dernier scan réussi : il y a Xh" / avertir si le dernier run a échoué | Lecture `authenticated`, écriture `service_role` only (aucune policy insert/update/delete pour `authenticated`/`anon`) |
| `business_items` | **Orpheline.** Reliquat de l'ancien module BusinessOS (supprimé du code avant l'audit de juillet 2026) — **contient encore 53 lignes de données réelles**, jamais migrées vers `listings`/`expenses`. À trancher : migrer les données puis dropper, ou dropper directement si sans valeur. | RLS activé, CRUD complet — mais plus aucun code n'y touche |
| `business_expenses` | Orpheline elle aussi, vide (0 ligne) | RLS activé, CRUD complet, inutilisée |
| `community_content` | **Espace Communauté, Lot 1-2** (2026-07-27) : unifie changelog/tutorial/guide/resource/faq (`type`), un seul statut brouillon/publié pour les 5. `category`/`sort_order` dès le départ (regroupement Notion-like Tutoriels/Guides/FAQ), `resource_kind`/`resource_url` nullables (pertinents pour `type='resource'` seulement), `discord_channel_id`/`discord_message_id`/`discord_synced_at` nullables (tolérance de schéma pour un futur cross-post Discord, non construit), `min_plan` nullable (2026-07-27, tolérance de schéma pour un futur gating par palier). `published_at` n'est jamais réécrit une fois posé (republier un item déjà publié ne le fait pas remonter dans le fil) | `select` : `status = 'published' or is_admin()`. `insert`/`update`/`delete` : `is_admin()` uniquement, via la fonction partagée `is_admin()` (lit `profiles.role` de l'appelant) |
| `roadmap_items` | **Espace Communauté, Lot 3** (2026-07-27) : roadmap publique, colonnes Now/Next/Later côté UI mappées sur `status` (`planned`/`in_progress`/`shipped`) — pas de brouillon/publié, un item est visible dès sa création | `select` : ouvert à tout `authenticated` (pas de notion de brouillon ici). `insert`/`update`/`delete` : `is_admin()` uniquement |
| `polls` / `poll_options` / `poll_votes` | **Espace Communauté, Lot 4** (2026-07-27) : sondages admin, un vote par utilisateur par sondage (`unique(poll_id, user_id)`, anti-spam en base). `poll_options.votes_count` dénormalisé, maintenu par le trigger `bump_poll_option_votes_count` (`SECURITY DEFINER` — un votant normal n'a lui-même aucune policy `UPDATE` directe sur `poll_options`). **Premier usage de Supabase Realtime hors Centre des Actions** (`usePolls.ts`) | `polls`/`poll_options` : `select` ouvert à tout `authenticated` (même un sondage clos reste consultable), écriture `is_admin()` uniquement. `poll_votes` : **jamais lisible publiquement** (`user_id = auth.uid() or is_admin()`), insert/delete réservés à son propre vote et seulement si le sondage cible est encore `status = 'open'` (vérifié dans la policy, pas seulement côté client) |
| `suggestions` / `suggestion_votes` | **Espace Communauté, Lot 5** (2026-07-27) : **seul endroit de tout l'espace Communauté où la création n'est pas réservée à l'admin** — `insert_own_suggestions` ne vérifie que `author_id = auth.uid()`, jamais `is_admin()` (commenté explicitement dans la migration pour qu'un lot futur ne le gate pas par erreur). Seuls le statut et `admin_reply`/`admin_reply_at` sont modifiables, et uniquement par l'admin. Tri par `votes_count desc` (pas par date). Même mécanique compteur dénormalisé + trigger `SECURITY DEFINER` (`bump_suggestion_votes_count`) que les sondages | `select` : ouvert à tout `authenticated`. `insert` : `author_id = auth.uid()` (pas de garde admin). `update`/`delete` : `is_admin()` uniquement. `suggestion_votes` : même discipline que `poll_votes` (jamais public, seul son propre vote ou l'admin) |
| `support_tickets` / `ticket_messages` | **Espace Communauté, Lot 6** (2026-07-27) — **lot le plus sensible côté confidentialité de tout ce chantier** : strictement privé, jamais de lecture publique contrairement au reste de l'espace Communauté. Garde-fou anti-usurpation vérifié en base (`insert_own_ticket_messages`) : `is_admin_reply` ne peut valoir `true` que si le posteur est réellement `is_admin()` — vérifié live en prod post-push (`pg_policies.with_check`). `ticket_messages` est un journal immuable (pas d'update/delete), même discipline que `action_log_entries`. Trigger `touch_ticket_on_new_message` (`SECURITY DEFINER`) met à jour `support_tickets.updated_at` à chaque nouveau message, pour trier la file d'attente admin par activité récente | `support_tickets` : `select`/`update` : `user_id = auth.uid() or is_admin()` (le propriétaire peut clore son propre ticket). `insert` : `user_id = auth.uid()`. `delete` : `is_admin()` uniquement. `ticket_messages` : `select`/`insert` limités aux messages d'un ticket que l'utilisateur possède (ou l'admin) |

Les 7 tables ci-dessus réutilisent toutes la fonction partagée `public.is_admin()` (`20260727100000_add_community_content.sql`, lit uniquement `profiles.role` de l'appelant, pas de `SECURITY DEFINER` nécessaire) plutôt que ~20 copies indépendantes de `EXISTS (SELECT 1 FROM profiles WHERE id = auth.uid() AND role = 'admin')` à travers les policies.

## RPC (fonctions SECURITY DEFINER)

| Fonction | Rôle | Accès |
|---|---|---|
| `increment_usage(p_user_id, p_month)` | Incrémente le compteur d'analyses du mois | `authenticated` uniquement, vérifie `auth.uid() = p_user_id` en interne |
| `reserve_credit(p_user_id uuid) returns uuid` | **P-02 (2026-08-04), remplace `decrement_credit`.** Réserve un crédit IA : décrémente `profiles.credits` (lève `insufficient_credits` si déjà à 0) et crée une ligne `credit_reservations` `status='pending'`, retourne son id. Appelée par `analyze-clothing` via un client `service_role` distinct, **après** vérification du JWT côté edge function — `p_user_id` est l'id verifié par ce JWT, jamais une valeur fournie librement par le client | **`service_role` uniquement** (`REVOKE` total pour `anon`/`authenticated`/`PUBLIC`). Ne vérifie plus `auth.uid()` en interne (résoudrait à `NULL` sous un appel `service_role`, sans session utilisateur) — la garantie d'identité vient du fait qu'aucun rôle PostgREST client ne peut plus jamais l'appeler |
| `consume_credit_reservation(p_reservation_id uuid, p_user_id uuid) returns boolean` | **P-02 (2026-08-04).** Clôture une réservation après une génération réussie (`pending`→`consumed`) — ne touche jamais `profiles.credits` (déjà décrémenté à la réservation), sert uniquement à empêcher un remboursement ultérieur. Idempotent : un second appel sur une ligne déjà `consumed` ne fait rien et retourne `false`. `analyze-clothing` **bloque la réponse de succès** si cette fonction ne retourne pas strictement `true` (voir plus bas) | **`service_role` uniquement**. Vérifie `user_id = p_user_id` dans la clause `WHERE` de l'`UPDATE` (pas seulement une garde en amont) |
| `refund_credit_reservation(p_reservation_id uuid, p_user_id uuid) returns integer` | **P-02 (2026-08-04), remplace `refund_credit`.** Rembourse une réservation après un échec Gemini — ne recrédite `profiles.credits` que si la transition `pending`→`refunded` a réellement eu lieu (idempotent : un id déjà `consumed`/`refunded`, inexistant, ou n'appartenant pas à l'appelant renvoie `NULL` sans effet) | **`service_role` uniquement**. Vérifie `user_id = p_user_id` dans la clause `WHERE` de l'`UPDATE` |
| ~~`decrement_credit(p_user_id)`~~ / ~~`refund_credit(p_user_id)`~~ | **Supprimées le 2026-08-04** (`20260804140000_drop_legacy_credit_rpcs.sql`), remplacées par les 3 RPC ci-dessus. Déploiement en deux temps pour rester rétrocompatible pendant le redéploiement de `analyze-clothing` : la migration additive (`20260804130000`) a d'abord coexisté avec elles le temps de vérifier la nouvelle edge function en prod, avant leur suppression par une migration séparée une fois cette vérification faite en conditions réelles (génération réussie + échec contrôlé, compte de test jetable) | — |

**Faille corrigée en cours de revue (2026-08-04, avant tout déploiement)** : la conception initiale de `reserve_credit`/`consume_credit_reservation`/`refund_credit_reservation` gardait le modèle `decrement_credit`/`refund_credit` — accès `authenticated`, vérification `auth.uid() = p_user_id`. Deux problèmes trouvés en relisant le flux avant de pousser : (1) `analyze-clothing` ne clôturait jamais la réservation sur le chemin de succès, donc `refund_credit_reservation` restait appelable pour toujours après coup ; (2) même corrigé, `authenticated` restait exécutant légitime des 3 RPC et lecteur de sa propre ligne `credit_reservations` — un utilisateur pouvait lire le `reservation_id` d'une génération en cours (RLS SELECT) et la rembourser en parallèle depuis les DevTools pendant que Gemini répondait encore. Design final : RPC réservées à `service_role` (jamais atteignables depuis le navigateur, quel que soit le `reservation_id` connu), `p_user_id` fourni explicitement par l'edge function post-JWT, `credit_reservations` sans aucune policy cliente, et `analyze-clothing` qui **refuse de renvoyer le résultat généré** si `consume_credit_reservation` ne confirme pas `true`.
| `set_default_vinted_account(target_account_id)` | **Phase B multi-comptes** (2026-07-09) : bascule le compte Vinted par défaut de l'utilisateur de façon atomique (désactive l'ancien, active le nouveau dans la même transaction). Un index partiel `unique (user_id) where is_default` sur `vinted_accounts` garantit qu'un seul compte par défaut peut exister même en cas d'appel concurrent. | `authenticated` uniquement, vérifie que le compte cible appartient à `auth.uid()` en interne |
| `allocate_next_sku(p_user_id)` | **Sprint extension V1** (2026-07-13) : calcule le plus petit entier positif non utilisé par une annonce active (`status <> 'vendu'` et `vinted_status` distinct de `'deleted'`) de l'utilisateur — une annonce vendue/supprimée libère implicitement son numéro sans jamais réécrire le `sku` déjà enregistré sur cette ligne. Verrou `pg_advisory_xact_lock(hashtext(p_user_id::text))` pour qu'un double insert concurrent ne calcule jamais le même numéro. Appelée par le trigger `trg_assign_sku_before_insert` sur `listings` (`BEFORE INSERT`, seulement si `NEW.sku is null` — un import qui reprend un numéro déjà présent dans le titre Vinted fournit son propre `sku` et court-circuite l'allocation) | `authenticated` uniquement, vérifie `auth.uid() = p_user_id` en interne |
| `handle_new_user()` | Trigger sur `auth.users`, crée le profil à l'inscription | Trigger uniquement, aucun accès RPC direct |
| `is_admin()` | **Espace Communauté** (2026-07-27) : `select exists (select 1 from profiles where id = auth.uid() and role = 'admin')` — fonction SQL partagée (pas `SECURITY DEFINER`, lit une ligne déjà autorisée par la policy existante sur `profiles`) réutilisée par ~20 policies d'écriture admin à travers les 7 tables de l'espace Communauté | `authenticated` uniquement (`revoke` pour `anon`) |
| `bump_poll_option_votes_count()` / `bump_suggestion_votes_count()` | **Espace Communauté, Lots 4-5** : triggers `AFTER INSERT/DELETE` sur `poll_votes`/`suggestion_votes`, incrémentent/décrémentent le compteur dénormalisé (`votes_count`) sur la ligne parente. `SECURITY DEFINER` car l'utilisateur qui vote n'a lui-même aucune policy `UPDATE` directe sur `poll_options`/`suggestions` | Trigger uniquement |
| `touch_ticket_on_new_message()` | **Espace Communauté, Lot 6** : trigger `AFTER INSERT` sur `ticket_messages`, met à jour `support_tickets.updated_at`. `SECURITY DEFINER` pour éviter de dupliquer indéfiniment la même condition de propriété que la policy `update_own_support_tickets` | Trigger uniquement |

`decrement_credit`/`increment_usage` étaient à l'origine appelables par le rôle `anon` **sans aucune vérification d'identité** — n'importe qui, non connecté, pouvait vider les crédits de n'importe quel utilisateur en boucle en devinant/énumérant des UUID. Corrigé en juillet 2026 (migration `20260708143000_secure_credit_rpcs.sql`) : accès restreint à `authenticated`, vérification `auth.uid() = p_user_id` ajoutée dans le corps des deux fonctions, `search_path` fixé (bonne pratique Postgres pour les fonctions `SECURITY DEFINER`).

## Verrouillage serveur du plan / crédits / abonnement (P0.1, 2026-07-11)

L'audit pré-lancement a confirmé en direct (via `npx supabase db query --linked`, en simulant un rôle `authenticated` réel dans une transaction annulée) qu'un utilisateur pouvait exécuter `supabase.from('profiles').update({plan:'pro', credits:999999})` depuis la console du navigateur et que ça fonctionnait — RLS protège les **lignes**, pas les **colonnes**, et rien ne restreignait quelles colonnes `authenticated` pouvait écrire sur `profiles`.

**Principe retenu, à réutiliser pour toute future colonne sensible** : les privilèges Postgres au niveau colonne (`GRANT`/`REVOKE UPDATE (colonne) ON table ...`) s'ajoutent à la RLS, ils ne la remplacent pas. Une colonne "sensible" (dont la valeur ne doit jamais dépendre de la volonté du client) doit avoir son `UPDATE` révoqué pour `authenticated`, et n'être modifiable que par une fonction `SECURITY DEFINER` qui vérifie `auth.uid() = p_user_id` en interne (tourne avec les privilèges du propriétaire de la fonction, donc pas affectée par le `REVOKE`) — ou, une fois Stripe branché, par un webhook en `service_role`.

**Piège rencontré en l'appliquant** : `REVOKE UPDATE (colonne) ON table FROM authenticated` **ne fait rien** si le privilège avait été accordé au niveau de la **table entière** (`GRANT UPDATE ON table TO authenticated`, sans liste de colonnes) — Postgres stocke les ACL de table (`relacl`) et de colonne (`attacl`) séparément, et un `REVOKE` colonne par colonne ne peut pas retirer un droit accordé au niveau table. `information_schema.column_privileges` affiche quand même la colonne comme "autorisée" dans ce cas (elle combine les deux ACL), ce qui peut faire croire à tort qu'un `REVOKE` colonne a fonctionné. Repéré sur `usage.analyses_count` (corrigé par `20260711091500_fix_usage_table_level_grant.sql`, un `REVOKE UPDATE` complet au niveau table) après que le premier essai colonne par colonne (`20260711090000_lock_billing_columns.sql`) n'ait silencieusement rien changé — vérifié après coup via `information_schema.table_privileges` (qui, lui, distingue vraiment table vs colonne). **Toujours vérifier via une vraie tentative de contournement (`SET LOCAL ROLE authenticated; SET LOCAL request.jwt.claims = '{"sub":"..."}'; UPDATE ...; ROLLBACK;`) plutôt que de faire confiance à `information_schema.column_privileges` seul.**

`profiles` (colonne `full_name` seule autorisée) et `subscriptions` (entièrement lecture seule pour le propriétaire) ont utilisé un `REVOKE UPDATE ON table FROM authenticated` complet suivi d'un `GRANT` ciblé sur les colonnes légitimes — ce schéma fonctionne du premier coup, contrairement au `REVOKE` colonne par colonne isolé.

`analyze-clothing` (fonction Edge) applique désormais le quota côté serveur de façon atomique : réservation (`reserve_credit`) **avant** l'appel Gemini si le plan est `free`, clôture par `consume_credit_reservation` **seulement après succès confirmé** (l'`id` de réservation n'est jamais renvoyé au client), remboursement (`refund_credit_reservation`) si Gemini échoue après coup, incrémentation (`increment_usage`) seulement après succès. Le client (`GeneratorPage.tsx`) ne fait plus aucun appel RPC de crédit lui-même — il se contente de rafraîchir le profil après une génération réussie.

## Storage

Bucket `listing-images` (photos des annonces). **Créé hors du flux `db push` avant le 2026-07-13** (aucune migration versionnée ne le créait, exactement le piège documenté en tête de ce fichier) **et jamais réellement utilisé par du code** avant cette date — `GeneratorPage.tsx` enregistrait directement les `blob:` URL du navigateur dans `listings.image_urls`, qui cassaient dès la fermeture de l'onglet d'origine. `supabase/migrations/20260713100000_add_listing_photos_bucket.sql` versionne enfin sa création (`on conflict do nothing`, idempotent que le bucket existe déjà ou non) et ajoute des policies `insert`/`update`/`delete` scopées par dossier `{user_id}/...` ; `src/lib/storage.ts::uploadListingPhotos()` y écrit réellement désormais.

**Corrigé le 2026-07-23** (`20260723130000_remove_listing_images_public_list_policy.sql`) : la policy `listing_images_public_read` (`select` accordé à `public` sans restriction de ligne) permettait de **lister** tous les fichiers du bucket, pas seulement d'y accéder par URL connue, signalé par `supabase db advisors`. Supprimée sans casser l'affichage réel : le bucket reste `public = true`, et `getPublicUrl()` (seule méthode utilisée par `src/lib/storage.ts`, confirmé par grep sur `src/`/`extension/` — aucun `.list()` ni `.download()`) contourne entièrement les RLS de `storage.objects` pour un bucket public.

## Vérifier l'état réel de la prod

```bash
npx supabase link --project-ref <ref>
npx supabase db query --linked "SELECT * FROM pg_policies WHERE tablename = '...';"
npx supabase db advisors --linked --type all   # audit securite/perf integre Supabase
```

`db advisors` a été la source de la plupart des trouvailles de sécurité de juillet 2026 (RLS manquantes, fonctions sans `search_path`, policies dupliquées, bucket storage trop permissif). À relancer périodiquement, surtout après tout ajout de table ou de RPC.
